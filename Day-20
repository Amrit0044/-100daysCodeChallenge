Write a program to find the product of odd digits of a number.


#include <stdio.h>

int main() {
    int num, digit;
    int product = 1;
    int found_odd = 0; // To track if there is at least one odd digit

    printf("Enter a number: ");
    scanf("%d", &num);

    if (num == 0) {
        printf("Product of odd digits: 0\n");
        return 0;
    }

    num = (num < 0) ? -num : num; // Handle negative numbers

    while (num > 0) {
        digit = num % 10;
        if (digit % 2 == 1) {  // check if digit is odd
            product *= digit;
            found_odd = 1;
        }
        num /= 10;
    }

    if (found_odd) {
        printf("Product of odd digits: %d\n", product);
    } else {
        printf("No odd digits found.\n");
    }

    return 0;
}


Write a program to find the 1’s complement of a binary number and print it.



#include <stdio.h>
#include <string.h>

int main() {
    char binary[100];
    
    printf("Enter a binary number: ");
    scanf("%s", binary);
    
    // Find length of the binary number
    int length = strlen(binary);
    
    // Calculate 1's complement
    for (int i = 0; i < length; i++) {
        if (binary[i] == '0') {
            binary[i] = '1';
        } else if (binary[i] == '1') {
            binary[i] = '0';
        } else {
            // Invalid character in binary string
            printf("Invalid binary number.\n");
            return 1;
        }
    }
    
    printf("1's complement: %s\n", binary);
    
    return 0;
}

